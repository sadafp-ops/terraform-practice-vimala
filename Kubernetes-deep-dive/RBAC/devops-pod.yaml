apiVersion: v1
kind: Pod
metadata:
  name: devops-app
  namespace: prod-app
spec:
  serviceAccountName: devops-sa
  containers:
  - name: kubectl
    image: bitnami/kubectl
    command: ["sleep", "3600"]
 
