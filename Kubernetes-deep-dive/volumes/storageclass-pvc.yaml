apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-storageclass
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: vimala-storage

